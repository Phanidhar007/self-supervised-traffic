# Core dependencies (all verified installed in the reference environment).
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
torch>=2.0          # CPU build is fine; small transformer encoder
streamlit>=1.30
jupyter>=1.0

# Optional / not required to run the pipeline or demo (fallbacks implemented):
# transformers>=4.40    # not needed -- we use our own small transformer encoder
# opacus>=1.4           # not needed -- no differential privacy in this project
# shap>=0.45            # not needed -- no shapley explanations here
# tenseal>=0.3          # not needed -- no homomorphic encryption here
flask>=3.0
